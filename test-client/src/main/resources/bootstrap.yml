server:
  port: ${PORT:8081}
spring:
  application:
    name: test-client
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
abc:
  name: LSIR